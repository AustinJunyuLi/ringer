{
  "run_name": "qwenclaude-lane-probe",
  "workdir": "<HOME>/.ringer/e2e-work/qwenclaude-probe",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "qwenclaude--executor-smoke",
      "engine": "qwenclaude",
      "model": "qwen3.8-max-preview",
      "task_type": "executor-probe",
      "timeout_s": 900,
      "expect_files": ["stats.py", "answer.txt"],
      "spec": "You are one worker in a lane probe. Write only inside your current task directory.\n\nTASK: Write ./stats.py defining a function median(xs) that returns the median of a non-empty list of numbers (mean of the two middle values for even length). Then write ./answer.txt containing ONLY the median of [3, 1, 4, 1, 5, 9, 2, 6] as a number — no prose.\n\nHARD RULES: exactly two files (stats.py, answer.txt), nothing else. No tests, no README. The executed check is the only verdict.",
      "check": "python3 <HOME>/.ringer/probes/qwenclaude/check_probe.py",
      "verified": "median() passes 3 hidden cases; answer.txt == 3.5; no extra files."
    }
  ]
}
